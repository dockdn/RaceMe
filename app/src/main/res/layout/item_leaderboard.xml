<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFF8F2"
    android:padding="16dp">

    <!-- header title -->

    <Button
        android:id="@+id/btnBackLeaderboard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back" />


    <TextView
        android:id="@+id/tvLeaderboardTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Friends leaderboard"
        android:textSize="22sp"
        android:textStyle="bold"
        android:fontFamily="@font/vt323"
        android:textColor="#B64D4D"
        android:gravity="center_horizontal"
        android:layout_marginBottom="4dp" />

    <!-- header subtitle -->

    <TextView
        android:id="@+id/tvLeaderboardSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Compare daily steps and miles with your friends."
        android:textSize="16sp"
        android:fontFamily="@font/vt323"
        android:textColor="#6B2C2C"
        android:gravity="center_horizontal"
        android:layout_marginBottom="12dp" />

    <!-- sort row: label + radio buttons -->

    <LinearLayout
        android:id="@+id/layoutSortRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/tvSortLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sort:"
            android:textSize="16sp"
            android:fontFamily="@font/vt323"
            android:textColor="#6B2C2C"
            android:layout_marginEnd="8dp" />

        <RadioGroup
            android:id="@+id/rgSort"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/rbSortDistance"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Distance"
                android:checked="true" />

            <RadioButton
                android:id="@+id/rbSortSteps"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Steps"
                android:layout_marginStart="16dp" />
        </RadioGroup>
    </LinearLayout>

    <!-- loading spinner -->
    <!-- (testing extra feature) -->

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:visibility="gone"
        android:layout_marginBottom="8dp" />

    <!-- leaderboard list -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvLeaderboard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
</LinearLayout>
